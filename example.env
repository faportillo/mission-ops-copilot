# Example environment configuration for Mission Ops Copilot
# Copy this file to ".env" and adjust values as needed.

# Runtime
NODE_ENV=development
LOG_LEVEL=info
PORT=3000

# Data backend: one of "in-memory" | "file" | "postgres"
# - in-memory: volatile, no persistence
# - file: JSON files under DATA_DIR
# - postgres: uses Prisma with DATABASE_URL
DATA_BACKEND=in-memory

# File backend settings
# Only required when DATA_BACKEND=file
# DATA_DIR=./data

# Postgres backend settings
# Only required when DATA_BACKEND=postgres
# Local CLI / tools:
# DATABASE_URL=postgresql://mission:mission@localhost:5432/mission_ops?schema=public
# Inside docker-compose network (containers use the service name "db" as host):
# DATABASE_URL_CONTAINER=postgresql://mission:mission@db:5432/mission_ops?schema=public

# Postgres container credentials (used by docker-compose)
POSTGRES_DB=mission_ops
POSTGRES_USER=mission
POSTGRES_PASSWORD=mission

# Optional LLM API key (stub client works without this)
# OPENAI_API_KEY=


